# Standup Skipper

Generates believable-but-meaningless standup updates so you can skip the meeting. Run: `python standup_skipper.py` and copy-paste the output. Your secret is safe with us (and the 10,000 other developers using this).